{
  "name": "darling-3",
  "version": "1.0.0",
  "description": "use buttons to move to diffrent pages of a help command cache all unbans and unmutes get a mysql database set up for darling only store temp mutes and bans to the database redo the cooldown to round up and not down. also use ms and not hm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^13.4.0",
    "express": "^4.17.2",
    "ms": "^2.1.3"
  },
  "devDependencies": {
    "node": "^16.13.1"
  }
}
